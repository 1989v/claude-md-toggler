{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main toggler window",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
